# TP XPath 03

Effectuez les requêtes suivantes sur le fichier `gdpBrice1684.tei.xml`

- recherchez tous les éléments `objectName` qui contiennent `"chapelle"`
- recherchez tous les éléments `date` dont l'attribut `@when` commence par `"16"`
- effectuez une concaténation des éléments `forename` et `surname` du premier `respStmt`
- modifiez cette expression pour calculer la longueur de la chaîne de caractères concaténée
- recherchez les valeurs uniques des attributs `@xml:lang`
